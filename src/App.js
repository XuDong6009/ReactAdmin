import React ,{Component} from "react";
import { Button } from 'antd';

export default class App extends Component {
    render() {
        return (
            <div>
                App描述
                <Button>点击按钮</Button>
            </div>
        );
    }
}
